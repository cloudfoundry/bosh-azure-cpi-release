meta: ~

stemcell: &stemcell
  name: bosh-azure-hyperv-ubuntu-trusty-go_agent
  version: REPLACE_WITH_STEMCELL_VERSION

compilation:
  cloud_properties:
    instance_type: (( merge || "Standard_D2_V2" ))

networks: (( merge ))

resource_pools:
- name: mysql_z1
  stemcell: *stemcell
  cloud_properties:
    instance_type: (( merge || "Standard_D2" ))

- name: mysql_z2
  stemcell: *stemcell
  cloud_properties:
    instance_type: (( merge || "Standard_D2" ))

- name: mysql_z3
  stemcell: *stemcell
  cloud_properties:
    instance_type: (( merge || "Standard_D2" ))

- name: proxy_z1
  stemcell: *stemcell
  cloud_properties:
    instance_type: (( merge || "Standard_D1" ))

- name: proxy_z2
  stemcell: *stemcell
  cloud_properties:
    instance_type: (( merge || "Standard_D1" ))

- name: cf-mysql-broker_z1
  stemcell: *stemcell
  cloud_properties:
    instance_type: (( merge || "Standard_D1" ))

- name: cf-mysql-broker_z2
  stemcell: *stemcell
  cloud_properties:
    instance_type: (( merge || "Standard_D1" ))

- name: errands_z1
  stemcell: *stemcell
  cloud_properties:
    instance_type: (( merge || "Standard_D1" ))

disk_pools:
- name: mysql-persistent-disk

properties:
  template_only: (( merge ))
